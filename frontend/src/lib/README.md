# lib

Pure helpers: date and SLA math, risk derivation, formatting, sorting. No React, no JSX, no imports
from `src/components/`.

Everything here should be trivially unit-testable — write the test alongside it (`format.test.ts`).
