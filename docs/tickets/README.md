# Tickets

One file per unit of work, sized to a single focused sitting and a single PR. Copy `TEMPLATE.md` to
`<AREA>-<n>-<slug>.md`, e.g. `ORD-1-lifecycle-table.md`.

Every ticket names the files it will touch. That is how two people working in parallel find a
collision before the merge conflict.
