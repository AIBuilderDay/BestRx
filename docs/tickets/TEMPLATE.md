# <TICKET-ID>: <short imperative title>

**Spec:** [../specs/<area>.md](../specs/<area>.md)
**Status:** todo | in progress | in review | done
**Branch:** `feat/<area>-<slug>`

## Goal

One or two sentences. What is true after this ticket that is not true now?

## Context an agent needs

Only what is not obvious from the spec: the pattern to follow, the component to reuse, the gotcha in
the data. Link, don't restate.

## Files

- `frontend/src/views/...` — create
- `frontend/src/components/orders/...` — create
- `frontend/src/lib/...` — modify

Naming the files up front is how we catch two tickets colliding before the merge conflict.

## Steps

1. Small, ordered, each independently verifiable.

## Acceptance criteria

- [ ] Observable and checkable.
- [ ] `task test` passes.
- [ ] The changed path was exercised in the running app.

## Out of scope

- What this ticket deliberately leaves alone.
