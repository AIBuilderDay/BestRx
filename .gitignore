node_modules/
dist/
.vite/
*.local
*.log
.DS_Store
.env
.env.*
!.env.example
.idea/
.vscode/*
!.vscode/extensions.json
coverage/

# Future backend
__pycache__/
*.egg-info/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Build and tooling artifacts
*.tsbuildinfo
.pnpm-store/

# Terraform. State can hold resource metadata and secret values — never commit it.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
!terraform.tfvars.example
# Zipped Lambda packages, produced by archive_file.
infra/modules/*/.build/

# Lambda deployment package, produced by notification-service/scripts/build.sh.
notification-service/build/
# Fixtures copied from frontend/src/data at build time. The frontend's copy is the original.
backend/data/
